<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="villablanca.agg.ProyectoPMDM">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VistaReservasUsuario"></activity>
        <activity android:name=".AlergiasUsuario" />
        <activity android:name=".HacerReserva" />
        <activity android:name=".CalendarioReserva" />
        <activity android:name=".RestauranteBuscado" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".BusquedaRestaurante" />
        <activity android:name=".MenuUsuario" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>